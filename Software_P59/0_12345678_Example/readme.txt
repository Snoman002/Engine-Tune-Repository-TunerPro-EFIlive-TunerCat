Folder Structure

XDF for OSID is placed in this top level folder

BIN files are placed individually in their own folder

-Folder Name is OSID_VIN_bin
-Inside each folder is the .bin file and the details "card" (readme.txt)
--use https://vpic.nhtsa.dot.gov/decoder/Decoder for vin details

**Only ONE bin file per folder to allow for indexing